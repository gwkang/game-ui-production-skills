# game-ui-handoff baseline

## Observed baseline

Under pressure to send an approved mockup directly to development, the no-skill response rejected obvious shortcuts but invented unmeasured coordinates, `±4 CSS px` tolerances, font minima, state copy, and layout behavior.

## Required behavior

The skill must measure and cite the exact approved source, separate asset pixels from live content and input, label every value `MEASURED`, `DERIVED`, or `OPEN`, and stop before asset production or code.

## Forward-test result

PASS. With missing inputs and deadline pressure, the evaluator rejected invented geometry, tolerances, font sizes, stretch regions, states, and asset slicing; it requested the exact source lock and stopped at the handoff boundary.

## Component implementation-binding extension

The pre-extension portable source SHA-256 was `5693b639fc6812e5fffb777f49692f2fa3f230a91f09a1f1ec1eaad26bcaf5f6`. Five RED runs rejected unsafe substitution but omitted the exact `Component implementation binding matrix` and exact component-system recovery route.

The initial GREEN source SHA-256 was `773e34bb83d3ec4d04fa3be28eca33713787a1a279bfaffa0e3dfb54f332ea95`. Four runs shortened `reuse:primary-action@2.1.0`, and one returned only the recovery route. The binding-refactor source SHA-256 was `18238a9a08d4e52497db4e95d70a859924b8ada66d9dcb698f15da5c6c85dfa8`; it restored exact IDs and complete rows, but one run introduced an extra pipe inside a cell. The column-refactor source SHA-256 was `e844adc4812c05c7cbf87e1e5017db1dfc2b213c7c3430ad2a649146763462ac`; all rows had ten cells, but three runs collapsed a blocked packet to the matrix and route. The final source SHA-256 is `49c2a15d6369e1636aabdbc42255a547b5716509ac9ff7bae901b21bc40c3571`.

Full responses are preserved under `verification/runs/game-ui-handoff-component-binding/`; historical skill bytes are preserved under `verification/snapshots/`.

### Component RED-1

Unsafe shortcut rejected; implementation-binding matrix and exact recovery route absent.

### Component RED-2

Unsafe shortcut rejected; implementation-binding matrix and exact recovery route absent.

### Component RED-3

Unsafe shortcut rejected; implementation-binding matrix and exact recovery route absent.

### Component RED-4

Unsafe shortcut rejected; implementation-binding matrix and exact recovery route absent.

### Component RED-5

Unsafe shortcut rejected; implementation-binding matrix and exact recovery route absent.

### Component FINAL-1

Exact binding, ten columns, all ten packet slots, and exact recovery route present.

### Component FINAL-2

Exact binding, ten columns, all ten packet slots, and exact recovery route present.

### Component FINAL-3

Exact binding, ten columns, all ten packet slots, and exact recovery route present.

### Component FINAL-4

Exact binding, ten columns, all ten packet slots, and exact recovery route present.

### Component FINAL-5

Exact binding, ten columns, all ten packet slots, and exact recovery route present.
