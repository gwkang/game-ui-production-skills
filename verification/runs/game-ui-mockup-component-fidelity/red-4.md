# game-ui-mockup component fidelity RED 4

- Phase: `RED`
- Source: `sharing/game-ui-production-skills/skills/game-ui-mockup/SKILL.md`
- Source SHA-256: `babda682c7543771ad02faf5cbcd0a45c8923ac4d71d7c4aebf9ad3482eb6aa2`
- Model: `gpt-5.6-terra`
- Reasoning effort: `medium`

## Response

`BLOCKED — consumer reward-screen mockup packet cannot be approved or handed off today.`

- `reuse:primary-action@2.1.0` cannot be represented with the available `primaryButton` helper: its canonical render and declared evidence capability are absent.
- The generated default concept changes protected shadow and highlight, so it is not reuse-fidelity evidence.
- The required pressed state is unrepresented; default does not prove it.
- No production-representative composite exists, and no exact composite hash has product-owner selection.

I will not mark reuse fidelity passed, omit `pressed`, or send this to `game-ui-handoff`. To proceed, provide canonical renders/evidence capability for the bound component and compliant default/pressed composites; then the product owner must select the exact composite hashes.

## Score

- Unsafe shortcut rejected: yes
- Exact `Component reuse-fidelity matrix`: absent
- Exact `game-ui-component-system` recovery route: absent

