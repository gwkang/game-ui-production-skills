# game-ui-runtime-validation baseline

## Observed baseline

The no-skill response rejected approval from one default screenshot and expanded coverage, but omitted a locked build fingerprint, complete target coverage, measured comparison evidence, font/runtime health, and boundary content values. Structural rectangle success could also mask composed-screen art mismatch.

## Required behavior

The skill must validate the unchanged actual runtime, bind raw evidence to the build, use risk-based coverage, test rendering/content/input/runtime health and composed fidelity separately, remain read-only, and never self-approve.

## Forward-test result

PASS. The evaluator rejected the partial evidence, preserved the mixed-style finding, remained read-only, refused self-approval, and required a fingerprint-locked rerun before independent acceptance review.

## Component runtime evidence pressure test

Five no-skill controls rejected inference from catalog and unit tests but emitted incompatible coverage records; one incorrectly routed missing captures to implementation. The first guided revision stabilized the matrix and route, while exposing copied-cell, title adjacency, and viewport-selection drift. The final revision indexed source cells by exact headers and copied coverage-map viewport strings verbatim. Five fresh final runs converged on three required rows and blocked acceptance.

### Component RED-1

Rejected inference; no stable matrix or route.

### Component RED-2

Rejected inference; omitted binding identity and exact route.

### Component RED-3

Rejected inference; collapsed coverage dimensions.

### Component RED-4

Rejected inference; misrouted missing captures to implementation.

### Component RED-5

Rejected inference; emitted an underspecified Markdown table.

### Component FINAL-1

PASS: exact copied fields, three selected coverage rows, and runtime recapture route.

### Component FINAL-2

PASS: exact copied fields, three selected coverage rows, and runtime recapture route.

### Component FINAL-3

PASS: exact copied fields, three selected coverage rows, and runtime recapture route.

### Component FINAL-4

PASS: exact copied fields, three selected coverage rows, and runtime recapture route.

### Component FINAL-5

PASS: exact copied fields, three selected coverage rows, and runtime recapture route.

## Final audit reconciliation

PASS: `final-4-declared-baseline-state.md` preserves the screen-spec-declared `idle` baseline at both targets without inventing `default`; `final-5-missing-baseline-block.md` returns an undeclared baseline to `game-ui-screen-spec`.
