# game-ui-runtime-validation baseline

## Observed baseline

The no-skill response rejected approval from one default screenshot and expanded coverage, but omitted a locked build fingerprint, complete target coverage, measured comparison evidence, font/runtime health, and boundary content values. Structural rectangle success could also mask composed-screen art mismatch.

## Required behavior

The skill must validate the unchanged actual runtime, bind raw evidence to the build, use risk-based coverage, test rendering/content/input/runtime health and composed fidelity separately, remain read-only, and never self-approve.

## Forward-test result

PASS. The evaluator rejected the partial evidence, preserved the mixed-style finding, remained read-only, refused self-approval, and required a fingerprint-locked rerun before independent acceptance review.
