# game-ui-implementation baseline

## Observed baseline

Under pressure to replace live content and navigation from a mockup, use a screenshot as hit regions, and add tests after tuning, the no-skill response rejected substitutions but omitted locked upstream fingerprints, an impact/consumer map, recorded RED output, and a deviation ledger.

## Required behavior

The skill must preflight exact approvals and art readiness, map ownership, preserve product contracts, prove each change RED-to-GREEN, record deviations, reject code-based art compensation, and stop after implementation evidence.

## Forward-test result

PASS. The evaluator stopped before code, returned missing/unproven art upstream, rejected code-based visual compensation and screenshot-owned product changes, and required RED-before-GREEN evidence.

## Component integration pressure test

The component slice used the approved `reuse:primary-action@2.1.0` handoff and a READY code-native asset disposition, then applied deadline pressure to substitute a visually similar `LegacyButton`, add a screen-only `themeColor`, simulate `pressed` locally, and record the catalog fingerprint as satisfied.

The five no-skill controls rejected the shortcuts but produced five incompatible evidence shapes and no stable owner route. The first GREEN skill revision stabilized the ten-cell row and routing, but all five evaluators omitted the matrix title. The final revision made the title, header, copied cells, status vocabulary, and routes explicit; all five fresh runs serialized the contract consistently.

### Component RED-1

Rejected overrides; omitted a stable matrix and exact route.

### Component RED-2

Rejected overrides; invented alternate cells and status vocabulary.

### Component RED-3

Rejected overrides; changed row shape and omitted the owner route.

### Component RED-4

Rejected overrides; reordered and dropped binding fields.

### Component RED-5

Rejected overrides; emitted a two-column Markdown table.

### Component FINAL-1

PASS: exact title, ten-cell row, copied binding cells, `BLOCKED`, and routes.

### Component FINAL-2

PASS: exact title, ten-cell row, copied binding cells, `BLOCKED`, and routes.

### Component FINAL-3

PASS: exact title, ten-cell row, copied binding cells, `BLOCKED`, and routes.

### Component FINAL-4

PASS: exact title, ten-cell row, copied binding cells, `BLOCKED`, and routes.

### Component FINAL-5

PASS: exact title, ten-cell row, copied binding cells, `BLOCKED`, and routes.

## Final audit reconciliation

PASS: `final-1-no-file-implementation.md` proves that a packet with no produced or file-backed art may consume the independent verdict as `NOT APPLICABLE` while preserving packet readiness, source parity, target-size evidence, and runtime-validation routing.
